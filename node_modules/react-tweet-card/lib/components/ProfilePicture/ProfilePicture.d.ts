declare type ProfilePictureProps = {
    image: string;
    clickableProfileLink?: boolean;
};
declare const ProfilePicture: ({ image, clickableProfileLink }: ProfilePictureProps) => JSX.Element;
export default ProfilePicture;
