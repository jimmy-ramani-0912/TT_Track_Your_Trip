declare type UserDetailsProps = {
    name: string;
    username: string;
    image: any;
    isVerified?: boolean;
    isProtected?: boolean;
    clickableProfileLink?: boolean;
};
declare const UserDetails: ({ name, username, image, isVerified, isProtected, clickableProfileLink, }: UserDetailsProps) => JSX.Element;
export default UserDetails;
