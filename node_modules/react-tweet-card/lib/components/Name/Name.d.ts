declare type NameProps = {
    name: string;
    clickableProfileLink?: boolean;
    isVerified?: boolean;
    isProtected?: boolean;
};
declare const Name: ({ name, clickableProfileLink, isVerified, isProtected, }: NameProps) => JSX.Element;
export default Name;
